# Implementation Plan: <Change name>

> **Deleted at archive.** This file records how the change was built, and that answer ages the moment
> the code moves on. Behaviour goes in the spec delta; interfaces go in `contracts/`, which survives.

**Spec**: [spec.md](./spec.md)

## Summary

<Two or three sentences: what changes, and the shape of the approach.>

## Technical Context

**Language / runtime**:
**Primary dependencies**:
**Storage**:
**Testing**:
**Target platform**:
**Performance / scale constraints**:

## Constitution Check — before research

<!--
GATE 1. Check the approach against the repo's constitution (path in sdd.config.yaml). A violation
means revising this plan, never reinterpreting the constitution. If a violation is genuinely
justified, record it in Complexity Tracking below with the simpler alternative you rejected.
-->

## Project Structure

<Which files and directories this change touches, by path.>

## Constitution Check — after design

<!-- GATE 2. Re-run it once the design is settled: the design is what violates a principle, not the
     summary written before it existed. -->

## Complexity Tracking

<!-- Only if a gate found a violation worth accepting. -->

| Violation | Why it is needed | Simpler alternative, and why it was rejected |
| --- | --- | --- |
